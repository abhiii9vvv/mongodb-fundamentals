# MongoDB CRUD Notes

Quick reference for basic CRUD operations in the MongoDB shell.

## Files
- `crud.mongodb.js`: example create/read/update/delete commands.

## Usage
1. Open MongoDB shell.
2. Run the script or copy commands as needed.

## Notes
- The examples assume a database named `CrudDB` and a collection named `courses`.
